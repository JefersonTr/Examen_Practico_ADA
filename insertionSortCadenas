def insertionSortCadenas(arr):
    for j in range(1, len(arr)):
        target = arr[j]
        i = j - 1

        while i >= 0 and int(arr[i]) > int(target):
            arr[i + 1] = arr[i]
            i -= 1
            
        arr[i + 1] = target
        
    return arr
